{% extends 'base.html.twig' %}

{% block body %}
    <p>{{location.name}}</p>
    <iframe width="420" height="315"src="{{location.videos}}"></iframe>
    <p>{{location.summary}}</p>
    <a href="{{location.links}}">Website</a>
    <a href="{{location.googleMap}}">Google Map</a>
{% endblock %}